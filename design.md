# BillNext 内容优先设计系统

更新：2026-09-18。目标平台：电脑浏览器插件，仅验收桌面鼠标与键盘交互，不新增移动端或触屏适配。

原则：**内容可以花，界面必须静。**

## 颜色

`tokens.css` 是基础令牌来源。`quiet-ui.scss` 在旧主题与首页布局之后加载，统一映射 `--bn-*` 与 `--billnext-*`，避免各页继续添加颜色覆盖。

- 页面：#F5F5F7；表面：#FFFFFF；搜索与次级表面：#F0F1F3；悬停：#EAEBEE。
- 主文字：#1D1D1F；次级文字：#6E6E73；视频元信息：#8A8A8E；弱装饰文字：#9A9AA0。
- 主蓝：#0A84FF，仅用于选中、键盘焦点及主要操作语义。
- 边界：rgba(0,0,0,.06)。移除大面积蓝色背景与棱镜环境光。
- 深色模式使用中性黑灰，并保留清晰的文字、菜单和焦点状态。

## 视频流

- 默认无卡片背景、边框、阴影或整体上浮；只让封面轻微响应悬停。
- 封面圆角 10px。标题最多两行，14px / 20px、500 字重。
- 元信息为一行：UP 主 · 播放量 · 日期，12px / 17px。移除常驻头像、关注按钮和推荐徽章。
- 元信息行固定高 28px，悬停或键盘聚焦时原位替换为“☆ 想看 / ✦ 帮我读”，已想看显示实心星；封面无操作遮挡。
- 鼠标右键直接触发不想看，不弹出菜单；处理中不重复触发。Shift+F10 保留键盘操作入口。
- 想看、帮我读、不想看、关注及资料库操作共用一个菜单；想看与帮我读统一使用中性灰，已想看通过实心星区分。保留处理中与禁用反馈。
- 首页保留一张主卡、三张紧凑卡和推荐画廊；主卡标题和元信息位于封面下方，不叠加遮罩。

## 导航与工具

- 收起侧栏宽 56px，图标 18px，点击区 36px，间隔 8px；中性表面，选中图标主蓝、底色 10% 主蓝。
- 默认收起，保留用户已经保存的展开偏好与展开入口。
- 搜索与工具区域最大宽度 728px，搜索框高 36px，取消外层大容器、边框与阴影；筛选和刷新紧邻搜索框。
- 本轮只改桌面呈现，不新增其他设备的适配规则。

## 形状与可访问性

- `--radius-small: 6px`：小按钮。
- `--radius-medium: 10px`：搜索、封面、菜单。
- `--radius-large: 14px`：侧栏和大容器。
- 键盘焦点即时显示 2px 蓝色轮廓。减少动态效果偏好关闭封面缩放。
- 菜单绝对定位，不改变卡片尺寸。业务逻辑与页面信息架构保持现有行为。

## 桌面布局系统

布局令牌统一定义于 `tokens.css`，首页、关注和资料库共享：

- 侧栏宽 56px（展开 196px），距窗口左侧 16px。
- 内容在侧栏之外的可用区域居中，最大宽 1480px，两侧至少 32px 留白。搜索工具栏跟随同一左边缘。
- 普通视频网格使用 auto-fill，最小列宽 260px，列距 16px，行距 32px；窗口缩放自动增减列数。保留空轨道，少量视频不拉伸，也不居中。
- 日期与视频网格左对齐，标题到网格 16px，分区间距 40px。
- 首页保留大图与紧凑推荐的特殊布局，下方普通网格与关注、收藏、稍后看统一。
- 标题 14px / 20px、500 字重；元信息 12px / 17px、#8A8A8E，全部来自共享 token。封面沿用统一的 10px 圆角。
- 日期使用 #9A9AA0；主卡文字移到封面下方，与普通卡片使用相同文字颜色。
- 菜单内边距 6px，阴影 0 4px 16px / 10%；不想看置底，以细线隔开，仅悬停时显示红色。

## 首页推荐标签

仅首页“更多推荐”网格中的普通卡片显示标签，顶部一张主卡与右侧三张紧凑卡不显示。标签在元信息行最左侧、UP 主前显示蓝色标签。已关注优先显示“已关注”；否则点赞数满一万时按整万向下取整显示“1万点赞”“2万点赞”等，不足一万或缺少数据不显示。标签随元信息一起在悬停时让位给快捷操作；关注页和资料库不显示。

## 首页首屏比例

保留 1 大 + 3 小，左右按 56:44 分配扣除间距后的空间。右侧三行共享 40% 封面列与 16px 图文间距，标题区严格对齐；主卡封面为 16:9，标题与元信息放在封面下方。四张共用元信息行悬停操作和右键不想看。首屏与更多推荐标题之间为 24px，标题到网格仍为 16px。

### 首屏精修 token

右侧使用独立列表，行距固定 28px，不随主图高度均分。缩略图占右侧 42%，文字最大宽度 300px；封面到文字 16px，主图到标题 12px，标题到元信息 6px，封面共用 10px 圆角。默认保持 56:44；PreviewApp 的 `screen=home&hero=compact` 参数切换至 53:47，供同内容 A/B 对照（主图宽度缩小约 5.4%），不影响正式页面。

### 固定推荐行

右侧三条统一为 124px 高，封面 220×124px，文字区与封面等高。标题保持最多两行；元信息／悬停操作行使用 margin-top:auto 贴底。外层左右间距 24px，图文间距 16px，右侧行距 28px；主图到标题 12px、标题到元信息 8px。不按主图高度拉伸右侧行距。
