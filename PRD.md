# BillNext 产品需求文档（PRD）

- 文档版本：v1.0
- 文档状态：讨论稿
- 更新日期：2026-08-27
- 产品形态：运行于哔哩哔哩网页内的桌面端浏览器扩展
- 当前重点：首页、动态

## 1. 产品概述

BillNext 是一个面向 B 站重度用户的视频发现与内容分拣工作台。

它不试图重新制作一个完整的 B 站，而是集中解决两个高频问题：

1. **首页发现**：把推荐视频清楚、漂亮地呈现出来，让用户快速判断什么值得看。
2. **动态清理**：把关注 UP 主的新视频变成一份可以逐条处理、最终清零的每日清单。

产品的核心价值不是“展示更多视频”，而是帮助用户更快做出内容决策：

- **想看**：保留到后续观看队列。
- **帮我读**：用 AI 提取内容价值，降低长视频判断成本。
- **不想看**：从当前内容流中移除，并保留可撤销能力。
- **关注管理**：在持续出现低质量内容时，帮助用户识别并取关低价值 UP 主。

## 2. 背景与问题

### 2.1 用户问题

B 站重度用户关注的 UP 主较多，每天会产生大量新视频。原生动态流更接近无限信息流，而不是待处理队列，因此用户容易遇到：

- 新视频持续堆积，不清楚还有多少内容没有判断。
- 已经判断“不感兴趣”的视频继续占据视觉空间。
- 想稍后观看、需要 AI 帮读和不想看的内容混在一起。
- 需要反复扫视大卡片，处理效率低。
- 长期关注低质量 UP 主，却缺少辅助判断和清理机制。
- 首页与动态承担不同任务，却使用相似的信息流和卡片语言。

### 2.2 当前产品问题

- 首页首条普通推荐被赋予过高的主视觉权重，内容重要性与视觉重要性不匹配。
- 动态页采用展示型视频卡片，缺少“待处理”“剩余数量”和“完成进度”的任务感。
- 操作按钮长期占据卡片空间，视觉噪声较高。
- 首页、动态和资料库之间虽然共享组件，但页面目标缺乏明确区分。
- 设计规范已经存在，但产品结构与视觉规则之间仍需建立更直接的映射。

## 3. 产品目标

### 3.1 核心目标

1. 用户进入首页后，能在 5 秒内理解视频内容和主要操作。
2. 用户进入动态后，能立即知道今天有多少条待处理视频。
3. 用户可以在不中断浏览节奏的情况下连续处理动态。
4. 每次操作都有即时、明确且可撤销的反馈。
5. 首页和动态拥有不同的信息密度与交互模型，但保持同一品牌语言。

### 3.2 成功结果

- 首页更适合发现内容，而不是管理任务。
- 动态更像可清零的视频收件箱，而不是第二个首页。
- 用户每天愿意主动打开动态并完成清理。
- 用户能逐步减少低价值关注，提高关注列表质量。

### 3.3 非目标

- 不替代 B 站视频播放页、评论区、投稿和社交功能。
- 不重新实现完整的 B 站账号与内容系统。
- 不在首个版本中提供复杂的社交推荐算法。
- 不为了视觉效果引入影响阅读效率的大型动画或装饰。
- 不根据一次“不想看”自动取关 UP 主。

## 4. 目标用户

### 4.1 核心用户

- 每天使用 B 站的桌面端用户。
- 关注数较多，每日动态视频数量较大。
- 把视频分为“值得看、需要摘要、不值得看”三类。
- 希望主动管理注意力，而不是被推荐流持续牵引。

### 4.2 典型任务

> 当我想找视频看时，我希望快速浏览高质量封面和关键信息，以便选出值得打开的视频。

> 当关注动态不断积累时，我希望像处理待办清单一样逐条做决定，以便当天把新视频处理干净。

> 当我无法判断长视频是否值得投入时间时，我希望先获得可靠摘要，以便减少试错成本。

> 当某个 UP 主持续发布我不感兴趣的内容时，我希望看到辅助信号，以便决定是否取关。

## 5. 产品原则

1. **先决策，后消费**：页面首先帮助用户判断，而不是诱导无限滚动。
2. **内容是主角**：封面、标题和作者优先，容器装饰保持克制。
3. **渐进式操作**：常用操作随时可达，低频操作按需出现。
4. **结果可逆**：隐藏、归档、关注变更等操作必须提供明确反馈和恢复路径。
5. **首页负责发现，动态负责清零**：不得再次将两页设计为相同卡片流。
6. **效率可以被感知**：动态页需要展示剩余量、处理进度和完成状态。

## 6. 信息架构

### 6.1 一级导航

- 首页
- 动态
- 追番
- 清单

### 6.2 个人内容

- 稍后再看
- 收藏
- 历史
- 不想看 / 回收站

### 6.3 工具入口

- 全局搜索
- 页面筛选
- 刷新
- 设置
- 原版 B 站入口

## 7. 首页需求

### 7.1 页面目标

首页用于发现和选择视频。用户应优先看到封面、标题、UP 主、时长和必要的数据，而不是复杂容器和长期显示的操作栏。

### 7.2 默认结构

- 使用稳定、统一的 16:9 视频网格作为主要结构。
- 普通推荐默认拥有相同视觉权重。
- 只有存在真实编辑推荐、排行榜第一或明确运营数据时，才允许使用大型主卡。
- 网格根据可用宽度自适应列数，保持稳定卡片宽度和间距。
- 页面支持持续加载，但必须保留明确的加载、失败和到底状态。

### 7.3 视频卡片信息

每张卡片必须展示：

- 视频封面
- 视频时长
- 标题，最多两行
- UP 主名称及头像
- 发布时间
- 播放量

可选展示：

- 点赞量
- 弹幕量
- 已关注状态
- 内容标签

### 7.4 卡片操作

- 默认状态不长期展示完整操作栏。
- 鼠标悬停或键盘聚焦后显示“想看、帮我读、不想看”。
- 操作出现时不得改变卡片尺寸或导致网格跳动。
- 点击标题或封面打开视频。
- “想看”成功后显示短反馈，并依据设置决定是否从首页隐藏。
- “不想看”后卡片退出，提供撤销入口。
- “帮我读”后显示生成状态，完成后可进入摘要结果。

### 7.5 搜索与筛选

- 支持 B 站全站搜索。
- 支持按视频最短时长筛选。
- 支持按发布时间筛选。
- 支持刷新推荐流。
- 当前筛选生效时必须有可见提示，并可一键清除。

### 7.6 首页视觉要求

- 参考 Airbnb 的网格秩序和留白，但采用 16:9 视频比例。
- 参考 Behance 的封面优先级、作者信息和悬停操作。
- 参考 Spotify 的导航层级和渐进式操作。
- 不采用 YouTube 式高噪声密集布局。
- 减少无意义的玻璃嵌套、边框和大面积阴影。

## 8. 动态需求

### 8.1 页面目标

动态是一份按时间组织、可以被清零的视频收件箱。用户进入页面后，应立即看到待处理总量、今天的数量和当前处理进度。

### 8.2 默认结构

- 内容按“今天、昨天、具体日期”分组。
- 日期分组默认按时间倒序排列。
- 每个分组显示待处理数量。
- 页面顶部显示总待处理数量与已处理进度。
- 默认采用比首页更紧凑的列表或紧凑媒体行，不使用大型商城式卡片。
- 缩略图用于识别内容，不承担主视觉功能。

### 8.3 单条动态内容

每条内容必须展示：

- 视频缩略图和时长
- 视频标题
- UP 主头像和名称
- 发布时间
- 必要的视频数据
- 关注状态
- 三个主要决策操作

当空间允许时，可增加右侧详情区，展示：

- 视频简介
- UP 主近期投稿
- 用户近期对该 UP 主执行“不想看”的次数
- 已关注时长或其他可用的关注关系信号

### 8.4 三向处理

#### 想看

- 将视频加入想看或稍后观看队列。
- 当前动态从待处理列表移除。
- 显示成功反馈和撤销操作。

#### 帮我读

- 发起 AI 摘要任务。
- 卡片显示排队、处理中、成功或失败状态。
- 用户可选择任务发起后立即移除，或等待摘要完成后再处理。
- 失败时保留原视频，并提供重试。

#### 不想看

- 将视频移入“不想看”或回收站。
- 当前动态从待处理列表移除。
- 显示撤销操作。
- 不得因为一次操作直接取关 UP 主。

### 8.5 连续处理体验

- 处理一条后，焦点自动移动到合理的下一条。
- 支持键盘选择上一条和下一条。
- 支持为三个主要动作配置快捷键。
- 快速连续操作时，页面不得频繁跳动或丢失当前位置。
- 列表重排采用短而稳定的动画，不使用弹跳效果。
- 支持减少动态效果的系统设置。

### 8.6 批量处理

- 支持进入多选模式。
- 支持按日期分组全选。
- 支持批量“想看”和批量“不想看”。
- 高风险批量操作必须展示目标数量。
- 批量处理完成后提供统一撤销或恢复入口。

### 8.7 清零状态

当某个日期分组处理完成时：

- 该分组折叠或离开待处理区域。
- 展示克制的完成反馈。

当全部动态处理完成时：

- 展示明确的“已处理完”状态。
- 显示本次处理数量。
- 提供刷新动态入口。
- 不自动填充推荐内容破坏清零感。

### 8.8 关注管理

- 每条动态允许进入 UP 主相关操作。
- 用户可以关注或取消关注。
- 取消关注前展示 UP 主名称和影响范围。
- 产品可以展示辅助信息，但最终取关必须由用户明确确认。
- 可在后续版本提供“低价值关注候选”，不得自动取关。

### 8.9 动态视觉要求

- 参考 Google Photos 的时间分组、多选和批量工具栏。
- 参考成熟邮件产品的归档、撤销和清零反馈。
- 页面密度高于首页，但标题和主要操作仍需清楚可读。
- 已处理、处理中和失败状态必须同时通过文字或图标表达，不能只依赖颜色。

## 9. AI 帮读

### 9.1 用户价值

帮助用户在不完整观看长视频的情况下，快速判断视频是否值得投入时间。

### 9.2 输出要求

- 一句话结论
- 主要观点
- 关键时间点（数据可用时）
- 是否值得观看的理由
- 明确区分视频原意与 AI 推断

### 9.3 状态要求

- 未开始
- 排队中
- 处理中
- 已完成
- 失败，可重试

任务状态应跨页面保持一致。用户离开当前页面后，任务不得无提示丢失。

## 10. 资料库与辅助页面

### 10.1 稍后再看、收藏、历史

- 复用统一的视频信息与操作语义。
- 支持网格和列表密度切换。
- 不复制动态页的待处理进度。
- 删除或移出列表必须提供明确反馈。

### 10.2 追番

- 展示用户正在追踪的番剧、观看状态和更新信息。
- 支持添加、编辑、打开和移除。
- 海报比例可独立于视频卡片，但沿用共享字体、颜色和交互状态。

### 10.3 清单

- 用于管理明确目标型内容，而不是临时动态。
- 支持完成状态、可用性状态和进度展示。
- 与动态页共享“完成反馈”，但保持独立的数据模型。

### 10.4 回收站

- 保存被“不想看”移除的内容。
- 支持单条恢复、全部恢复和清空。
- 清空属于不可逆或高风险操作，必须明确确认。

## 11. 通用交互与状态

### 11.1 必备状态

所有数据页面必须具备：

- 首次加载
- 增量加载
- 空状态
- 筛选后无结果
- 网络错误
- 操作处理中
- 操作成功
- 操作失败

### 11.2 反馈规则

- 普通成功反馈不得阻断操作。
- 错误反馈必须说明发生了什么以及用户下一步能做什么。
- 加载状态不得改变按钮或卡片的几何尺寸。
- 操作完成后应优先提供撤销，而不是频繁要求二次确认。
- 取关、彻底清空等影响较大的操作需要确认。

### 11.3 键盘与无障碍

- 所有主要操作可使用键盘完成。
- 焦点状态必须清晰可见。
- 操作按钮提供可理解的无障碍名称。
- 文字和背景满足合理的对比度要求。
- 不用颜色作为唯一状态信号。
- 页面缩放后仍能保持可用的阅读顺序。

## 12. 数据与隐私

- 默认复用用户现有的 B 站登录状态获取内容。
- 用户的本地偏好、处理记录和界面设置优先保存在本地。
- AI 帮读如需上传字幕、标题或视频信息，必须明确说明处理范围。
- 不收集与核心功能无关的浏览历史。
- 不自动执行关注关系变更。
- 数据删除与清空必须有清楚的范围说明。

## 13. 指标

### 13.1 核心指标

- 每日动态处理完成率
- 每次动态会话处理的视频数量
- 从打开动态到完成第一次决策的时间
- 单条动态平均处理时长
- 首页视频点击率
- “想看、帮我读、不想看”三类操作占比

### 13.2 质量指标

- 操作撤销率
- 帮读任务成功率与平均完成时间
- 数据加载失败率
- 用户误操作恢复率
- 页面布局跳动和交互错误数量

### 13.3 约束指标

- 不因追求清零而显著增加误删或误取关。
- 不因增加视觉效果而降低首屏加载和滚动性能。
- 不通过自动推荐填充破坏动态完成状态。

## 14. 优先级

### P0：建立核心产品差异

- 首页统一视频网格与清晰卡片层级
- 动态按日期分组
- 动态待处理数量与处理进度
- 想看、帮我读、不想看三向处理
- 操作后移除与撤销
- 首页和动态明确使用不同布局
- 完整加载、空、错误状态

### P1：提升处理效率

- 动态键盘导航和快捷键
- 多选与批量处理
- 清零完成状态
- UP 主详情与关注管理
- 帮读状态跨页面同步
- 过滤条件可见与一键清除

### P2：形成长期价值

- 低价值关注候选分析
- 处理行为统计
- 个性化但可解释的排序
- 更丰富的资料库视图
- 设计与交互偏好自定义

## 15. 验收标准

### 15.1 首页

- 首屏所有普通推荐默认保持相同视觉权重。
- 用户无需打开详情即可识别标题、作者、时长和发布时间。
- 三个决策操作可通过鼠标和键盘访问。
- 操作出现或消失不会引起卡片跳动。
- 不同桌面宽度下网格无横向溢出、遮挡和异常裁切。

### 15.2 动态

- 用户进入页面即可看到总待处理数量。
- 所有视频按日期归组，并显示分组剩余量。
- 三个核心操作均可在一次直接操作内完成。
- 处理完成的项目立即离开待处理队列。
- 用户可以撤销最近的处理操作。
- 处理一条后，下一条仍在可预测位置或自动获得焦点。
- 全部处理完成后展示稳定的清零状态。
- 取关操作必须由用户明确触发并确认。

## 16. 里程碑建议

### 里程碑 A：结构重塑

- 确认首页和动态的最终线框结构。
- 首页切换为统一视频网格。
- 动态切换为日期分组的紧凑处理列表。

### 里程碑 B：决策闭环

- 完成三向处理、撤销和回收站闭环。
- 加入剩余量、进度和清零状态。
- 验证快速连续处理不会丢失数据或焦点。

### 里程碑 C：效率与质量

- 加入键盘操作和批量处理。
- 完善帮读状态和失败恢复。
- 完成响应式、无障碍和性能检查。

## 17. 风险

- B 站页面结构或接口变化可能影响内容获取。
- 快速连续处理可能造成状态不同步或误操作。
- AI 摘要的延迟和准确性可能削弱“帮我读”的信任度。
- 过度模仿参考产品可能导致品牌缺乏自身辨识度。
- 功能不断增加可能再次让动态退化为高噪声内容页。

## 18. 待确认问题

1. “想看”最终写入 B 站稍后再看，还是维护 BillNext 独立队列？
2. “帮我读”发起后，动态应立即移除还是等待摘要成功？
3. 动态默认采用单列紧凑列表、双列媒体行，还是允许用户切换？
4. 不想看记录是否只作用于当前视频，还是参与后续推荐过滤？
5. 取关辅助信号需要统计哪些行为，统计周期多长？
6. 首页是否存在真正的编辑精选数据，还是完全使用算法推荐？
7. 动态清零是按当前筛选范围计算，还是按全部未处理内容计算？

